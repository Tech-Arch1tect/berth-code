{
  "name": "berth-code",
  "displayName": "Berth Stack Manager",
  "description": "Manage Docker stack files and configurations through Berth server",
  "version": "0.0.1",
  "publisher": "berth",
  "engines": {
    "vscode": "^1.104.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "berth.login",
        "title": "Login to Berth",
        "category": "Berth"
      },
      {
        "command": "berth.logout",
        "title": "Logout from Berth",
        "category": "Berth"
      },
      {
        "command": "berth.refreshStacks",
        "title": "Refresh Stacks",
        "category": "Berth",
        "icon": "$(refresh)"
      },
      {
        "command": "berth.createFile",
        "title": "New File",
        "category": "Berth",
        "icon": "$(new-file)"
      },
      {
        "command": "berth.createFolder",
        "title": "New Folder",
        "category": "Berth",
        "icon": "$(new-folder)"
      },
      {
        "command": "berth.deleteFile",
        "title": "Delete",
        "category": "Berth"
      },
      {
        "command": "berth.renameFile",
        "title": "Rename",
        "category": "Berth"
      },
      {
        "command": "berth.uploadFile",
        "title": "Upload File",
        "category": "Berth",
        "icon": "$(cloud-upload)"
      },
      {
        "command": "berth.downloadFile",
        "title": "Download File",
        "category": "Berth",
        "icon": "$(cloud-download)"
      },
      {
        "command": "berth.chmodFile",
        "title": "Change Permissions (chmod)",
        "category": "Berth",
        "icon": "$(lock)"
      },
      {
        "command": "berth.chownFile",
        "title": "Change Ownership (chown)",
        "category": "Berth",
        "icon": "$(account)"
      },
      {
        "command": "berth.openStackInExplorer",
        "title": "Open Stack in Explorer",
        "category": "Berth",
        "icon": "$(folder-opened)"
      }
    ],
    "views": {
      "berth": [
        {
          "id": "berthServers",
          "name": "Servers & Stacks",
          "visibility": "visible"
        }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "berth",
          "title": "Berth",
          "icon": "$(server)"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "berthServers",
        "contents": "Connect to a Berth server to manage your Docker stacks.\n\n[Login to Berth](command:berth.login)\n\nTo learn more about Berth, [read the documentation](https://github.com/tech-arch1tect/berth)."
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "berth.refreshStacks",
          "when": "view == berthServers",
          "group": "navigation@2"
        },
        {
          "command": "berth.createFile",
          "when": "view == berthServers",
          "group": "navigation@3"
        },
        {
          "command": "berth.createFolder",
          "when": "view == berthServers",
          "group": "navigation@4"
        },
        {
          "command": "berth.uploadFile",
          "when": "view == berthServers",
          "group": "navigation@5"
        }
      ],
      "view/item/context": [
        {
          "command": "berth.createFile",
          "when": "view == berthServers && viewItem == folder",
          "group": "1_creation"
        },
        {
          "command": "berth.createFolder",
          "when": "view == berthServers && viewItem == folder",
          "group": "1_creation"
        },
        {
          "command": "berth.uploadFile",
          "when": "view == berthServers && viewItem == folder",
          "group": "2_upload"
        },
        {
          "command": "berth.downloadFile",
          "when": "view == berthServers && viewItem == file",
          "group": "3_download"
        },
        {
          "command": "berth.renameFile",
          "when": "view == berthServers && viewItem =~ /^(file|folder)$/",
          "group": "4_edit"
        },
        {
          "command": "berth.chmodFile",
          "when": "view == berthServers && viewItem =~ /^(file|folder)$/",
          "group": "4_permissions"
        },
        {
          "command": "berth.chownFile",
          "when": "view == berthServers && viewItem =~ /^(file|folder)$/",
          "group": "4_permissions"
        },
        {
          "command": "berth.deleteFile",
          "when": "view == berthServers && viewItem =~ /^(file|folder)$/",
          "group": "5_delete"
        }
      ],
      "explorer/context": [
        {
          "command": "berth.createFile",
          "when": "explorerResourceIsFolder && resourceScheme == berth",
          "group": "1_creation"
        },
        {
          "command": "berth.createFolder",
          "when": "explorerResourceIsFolder && resourceScheme == berth",
          "group": "1_creation"
        },
        {
          "command": "berth.uploadFile",
          "when": "explorerResourceIsFolder && resourceScheme == berth",
          "group": "2_upload"
        },
        {
          "command": "berth.downloadFile",
          "when": "!explorerResourceIsFolder && resourceScheme == berth",
          "group": "3_download"
        },
        {
          "command": "berth.renameFile",
          "when": "resourceScheme == berth",
          "group": "4_edit"
        },
        {
          "command": "berth.chmodFile",
          "when": "resourceScheme == berth",
          "group": "4_permissions"
        },
        {
          "command": "berth.chownFile",
          "when": "resourceScheme == berth",
          "group": "4_permissions"
        },
        {
          "command": "berth.deleteFile",
          "when": "resourceScheme == berth",
          "group": "5_delete"
        }
      ],
      "commandPalette": [
        {
          "command": "berth.refreshStacks"
        }
      ]
    },
    "configuration": {
      "title": "Berth",
      "properties": {
        "berth.serverUrl": {
          "type": "string",
          "default": "https://localhost:8080",
          "description": "URL of the Berth server"
        },
        "berth.trustSelfSignedCertificates": {
          "type": "boolean",
          "default": true,
          "description": "Trust self-signed certificates (useful for development)"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "webpack",
    "watch": "webpack --watch",
    "package": "webpack --mode production --devtool hidden-source-map",
    "package-vsix": "npm run package && vsce package",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.104.0",
    "@typescript-eslint/eslint-plugin": "^8.42.0",
    "@typescript-eslint/parser": "^8.42.0",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "^3.6.0",
    "eslint": "^9.34.0",
    "ts-loader": "^9.5.4",
    "typescript": "^5.9.2",
    "webpack": "^5.101.3",
    "webpack-cli": "^6.0.1"
  }
}
